import React, { useState } from 'react';
import { Icon, Header, Button } from 'semantic-ui-react';

export default () => {
    <>
        <Header as='h1'>Turn on Text Message Notifications</Header>
        <Button title = {'Enable Text Message Notifs'} />
    </>
}